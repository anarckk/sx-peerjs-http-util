请求API的使用方式为

初始化
```js
export function PeerJsWrapper {
    // 创建 Peer 用于发送请求
    this.peerInstance = new Peer();
    return {
        // peerId 对端设备ID
        // path 请求地址
        // data 请求数据
        send: function (peerId, path, data) {
            // 每次发送消息的时候，都连接一个新的 conn
            ...
            // 返回值是 Promise<Response>
        }
    }
}
```

对端设备接收，通过 path 对数据进行处理。