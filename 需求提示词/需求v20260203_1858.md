写一个nodejs库工具，把浏览器端的PeerJS封装成简单易用的类似http的api。

封装api，底层是 peerjs，
入参是对端设备peerId和传递的request参数，返回值是 Promise<Response> 对端设备的返回值。
每次请求都重新连接conn。

要有e2e测试。

这个库不是给nodejs后端环境设计的，而是为浏览器设计的，未来仅提供给浏览器端使用。

使用typescript。

request、response不需要header，可以简化。