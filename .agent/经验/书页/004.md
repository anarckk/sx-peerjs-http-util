# 模块重命名 - 004

将 `Routing.ts` 重命名为 `Router.ts`，类名从 `RoutingManager` 改为 `Router`。

## 步骤
1. 重命名文件：`mv src/Routing.ts src/Router.ts`
2. 修改类名和注释
3. 更新所有引用（import、变量名）
4. 更新 `.agent/索引.md`

## 注意
- 使用 `replaceAll` 批量替换变量名
